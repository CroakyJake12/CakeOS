# Migration boundary

`CroakyJake12/HavenAI` is a preserved migration donor. Existing HUI and app-surface work is reviewed per commit and copied or adapted deliberately into `HUI/` and `apps/`; it is never merged wholesale into the operating-system root. GNOME, Mutter, image, packaging, and VM claims require evidence from their matching HavenOS paths.
