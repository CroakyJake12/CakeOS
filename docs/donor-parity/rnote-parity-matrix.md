# Rnote parity matrix — donor `flxzt/rnote @ 8cac558` (v0.14.2) vs CakeOS Canvas

Scope: faithful-HUI-reconstruction ground truth. Donor GUI = `crates/rnote-ui` at the pinned rev; donor backend = `crates/rnote-engine` + `crates/rnote-compose` at the same rev. CakeOS side = `apps/canvas/rnote-poc` (`src/lib.rs`, `src/ffi.rs`) + `HUI/{WindowsHost,LinuxHost}/Canvas/` (`CanvasToolStrip.cs`, `CanvasNativeBridge.cs`, `CanvasManagedBoundaryProof.cs`). GTK/Libadwaita embedding is explicitly forbidden by `apps/canvas/rnote-poc/PROVENANCE.md:19`; every `rnote-ui` GTK widget below therefore needs a HUI reconstruction — it is never "pass" by backend alone.

Status key: `PASS` = donor behaviour reachable end-to-end today. `PARTIAL` = engine-real subset only. `MISSING` = absent, no specific blocker beyond work. `BLOCKED BY BRIDGE` = engine API exists but the C ABI / `CanvasNativeSession` does not expose it. `BLOCKED BY HUI` = bridge/API path exists (or is trivially engine-internal) but no HUI surface exists to drive it.

| DONOR FEATURE | DONOR UI LOCATION | DONOR BEHAVIOUR | DONOR BACKEND/API | CAKEOS BRIDGE SUPPORT | CAKEOS CONTROLLER/MODEL SUPPORT | HUI SUPPORT | CURRENT CAKEOS UI STATUS | PARITY STATUS | NOTES |
|---|---|---|---|---|---|---|---|---|---|
| App window + header bar chrome | `rnote-ui/src/mainheader.rs` + `data/ui/mainheader.ui`; `appwindow/mod.rs`, `imp.rs` | Adw HeaderBar: title + unsaved `*` indicator, left/right sidebar reveal toggles, canvas-menu + app-menu buttons, quick-actions; resists accidental resize via capture controllers | `Engine`, `EngineConfig`; `canvas.output_file`, `doc_title_display` | None — no title/sidebar/menu bindings | None | Vendor `Haven.UI` has Page/Container/Text/Button primitives, no HeaderBar reconstruction | No CakeOS header bar, title, unsaved indicator, or sidebar toggles | BLOCKED BY HUI | Needs HUI header reconstruction; bridge has nothing to expose here |
| Pen picker strip (tool selector + undo/redo) | `rnote-ui/src/penpicker.rs` + `data/ui/penpicker.ui` | 6 toggle buttons (brush/shaper/typewriter/eraser/selector/tools) calling `appwindow.set_pen_style`, plus undo/redo buttons | `Engine::change_pen_style(PenStyle)` (`engine/src/pens/mod.rs:184-213`) | 5 of 6 styles via `cake_canvas_set_stroke_tool` (`ffi.rs:254-271`); no Typewriter(→InvalidArgument), no Tools | `CanvasTool` enum Pen/Highlighter/Eraser/Selector/Shape (`lib.rs:56-76`); `CanvasManagedBoundaryProof` exercises 4 of 5 | `CanvasToolStrip.cs:19-25` — 5 tool buttons + Undo/Redo, `SetTool`/`SetHistory` | Simplified strip exists (Pen/Highlight/Eraser/Select/Shape/Undo/Redo); no Typewriter, no Tools, no shortcuts | PARTIAL | Bridge+strip cover 5/6 tools; Typewriter+Tools missing at ABI level |
| Brush (pen) tool select | `penpicker.rs:112-120` (brush toggle) | Select brush pen; subsequent strokes are freehand ink | `Pen::Brush`, `PenStyle::Brush` | `CAKE_CANVAS_TOOL_PEN` → `PenStyle::Brush` + `BrushStyle::Solid` (`lib.rs:226-237`) | `CanvasTool::Pen`, `set_tool` with mid-stroke guard (`lib.rs:204-211`) | `PenButton` in `CanvasToolStrip` | Working: proof draws pressure-sensitive pen stroke, saves, reloads | PASS | Core ink path is engine-real, not emulated |
| Brush styles Marker / Solid / Textured | `penssidebar/brushpage.rs`; `engine/src/pens/pensconfig/brushconfig.rs:23-30` | Style picker: Marker (const pressure, w=12, highlighter layer), Solid (smooth), Textured (seeded) | `BrushStyle::{Marker,Solid,Textured}`, `MarkerOptions`/`SolidOptions`/`TexturedOptions` | Marker iff tool==Highlighter else Solid (`lib.rs:228-232`); Textured unreachable | `CanvasTool::Highlighter` maps to `Brush+Marker`; no style/seed API | No style picker UI | Highlighter works as Marker; Textured style + seed control absent | PARTIAL | BLOCKED BY BRIDGE for Textured/seed; HUI picker also missing |
| Brush stroke width / color / fill / pressure curve / path builder | `penssidebar/brushpage.rs`, `strokewidthpicker/`, `colorpicker/`; `rnote-compose` `SmoothOptions`/`TexturedOptions`, `PenPathBuilderType::{Simple,Curved,Modeled}` (`compose/builders/mod.rs:162-173`) | Width 0.1–500 (`brushconfig.rs:116-117`), stroke/fill color, pressure curve, path builder choice | `BrushConfig`, `PensConfig::set_all_stroke_colors/fill_colors` | None — `configure_tool` only flips Marker/Solid; width/color/fill/builder untouched (engine defaults) | None | No color/width picker wired to Canvas | No width/color/fill/pressure/builder UI or API | BLOCKED BY BRIDGE | Engine supports it; needs ABI + HUI pickers |
| Shaper tool + shape builders | `penpicker.rs:122-130`; `penssidebar/shaperpage.rs`; `compose/builders/mod.rs:55-96` (13 builders: Line/Arrow/Rectangle/Grid/CoordSystem2D/CoordSystem3D/QuadrantCoordSystem2D/Ellipse/FociEllipse/QuadBez/CubBez/Polyline/Polygon) | Pick builder, drag out shape; constraints (1:1/H/V) | `Pen::Shaper`, `ShapeBuilderType`, `ShaperConfig` | 4 of 13: Rectangle/Ellipse/Line/Arrow via `cake_canvas_set_shape` (`ffi.rs:273-291`, `lib.rs:88-97`) | `CanvasShape` 4-variant enum; proof draws Ellipse | `ShapeButton` selects Shape tool; no builder-choice UI | Shape drawing works for 4 builders; 9 builders unreachable | PARTIAL | Grid/coord-systems/beziers/polygon(s) need ABI + picker |
| Shaper smooth / rough / highlight mode+opacity / constraints | `penssidebar/shaperpage.rs`; `shaperconfig.rs:38-58` | Smooth vs Rough, highlight mode (alpha 0.45), constraint ratios | `ShaperStyle`, `ShaperConfig::gen_style_for_current_options` | None — builder_type only; style/options stay default | None | None | No style/highlight/constraint UI or API | BLOCKED BY BRIDGE | — |
| Typewriter tool + text styling | `penpicker.rs:132-140`; `penssidebar/typewriterpage.rs`; `typewriter/` pen + `typewriterconfig.rs` (TextStyle, width default 600) | Click-to-type text boxes; font/size/color/width; Bold/Italic/Underline actions (`actions.rs:114-118`) | `Pen::Typewriter`, `TypewriterConfig`, `TextStyle` | None — no `PenStyle::Typewriter` mapping; ABI rejects it | None | None | No text tool anywhere in CakeOS Canvas | BLOCKED BY BRIDGE | Largest tool gap; needs ABI + text-entry HUI |
| Eraser trash-colliding-strokes mode | `penpicker.rs:142-150`; `penssidebar/eraserpage.rs`; `eraserconfig.rs:10-15` | Eraser stroke trashes colliding strokes (default) | `EraserStyle::TrashCollidingStrokes` (default) + `Eraser` pen | Yes — `CAKE_CANVAS_TOOL_ERASER` → `PenStyle::Eraser`; `lib.rs` test `eraser_trashes_stroke_and_history_restores_state` proves trash+undo+redo | `CanvasTool::Eraser` | `EraserButton` | Working trash eraser with history restore | PASS | Width fixed at engine default; mode itself passes |
| Eraser split mode + eraser width | `penssidebar/eraserpage.rs`; `eraserconfig.rs:35-59` (width 1–500, default 12) | Split colliding strokes instead of trashing; width picker | `EraserStyle::SplitCollidingStrokes`, `EraserConfig::width` | None — style/width not exposed; always default trash @ default width | None | None | No split mode, no width control | BLOCKED BY BRIDGE | — |
| Selector styles (Polygon/Rectangle/Single/IntersectingPath) | `penssidebar/selectorpage.rs`; `selectorconfig.rs:18-27` (default Rectangle) | Lasso vs rect vs single-pick vs intersect-path selection gesture | `SelectorStyle`, `Selector::add_to_select_path` | None — selector style stays engine default (Rectangle); CakeOS lasso polygon stroke is interpreted as Rectangle-style gesture, not true Polygon mode | None | `SelectorButton` only selects tool | Selector stroke path works but style choice absent | PARTIAL | Proof draws closed polygon path; engine default Rectangle still selects. Needs style ABI |
| Selection actions (move/resize/rotate/duplicate/trash/select-all/deselect/invert-color) | `canvaswrapper.rs`; `selector/mod.rs` (ModifyState Move/Rotate/Resize, resize/rotate nodes, `select_all`, `cancel_selection`); `actions.rs:84-92` (selection-trash/duplicate/invert/select-all/deselect-all) | Manipulate selection: drag-move, corner resize (+aspect lock `resize_lock_aspectratio`), rotate handle, duplicate, trash, select/deselect all, invert color | `Selector` pen, `SelectionComp`, `TrashComp`, `StrokeComp` | None — no selection query/manipulation API; undo/redo only | None | None | No selection handles, no selection menu, no keyboard selection ops | BLOCKED BY BRIDGE | Selection exists inside engine but is unreachable from HUI |
| Selection clipboard copy / cut (SVG+PNG) | `selector/mod.rs:159-267` (`fetch_clipboard_content`, `cut_clipboard_content`: SVG + PNG w/ `CLIPBOARD_EXPORT_MARGIN`); `actions.rs:120-124` | Copy/cut selection to system clipboard as SVG and PNG | `StrokeContent::gen_svg/gen_image`, oneshot clipboard channels | None | None | None | No copy/cut of selection | BLOCKED BY BRIDGE | Needs ABI + OS clipboard (HUI-owned) |
| Tools tool (VerticalSpace/OffsetCamera/Zoom/Laser) | `penpicker.rs:162-170`; `penssidebar/toolspage.rs`; `toolsconfig.rs:18-27`; `tools/{verticalspace,offsetcamera,zoom,laser}.rs` | Layout tools: insert vertical space, pan camera, drag-zoom, laser pointer | `Pen::Tools`, `ToolStyle` (4), `VerticalSpaceToolConfig` | None — `PenStyle::Tools` unreachable via ABI | None | None | No layout/present tools | BLOCKED BY BRIDGE | Zoom-laser etc. conflated with camera pan/zoom API, which is separate (see Zoom row) |
| PenMode + stylus/drawing-pad/mouse/touch shortcuts | `penmode.rs`; `penholder.rs`; `shortcuts.rs:59-128` (StylusPrimary→Eraser tmp, StylusSecondary→Selector tmp, MouseSecondary→Shaper tmp, TouchTwoFinger→Eraser toggle, Ctrl+Space→Tools toggle, Pad0-3→Brush/Shaper/Typewriter/Eraser permanent); `actions.rs:136-147` pad actions | Hardware-button pen switching,incl. temporary/toggle/permanent modes | `PenMode`, `Shortcuts`, `ShortcutKey`, `ShortcutAction::ChangePenStyle` | None — `send_pen_event` always `PenMode::Pen`, empty modifiers (`lib.rs:194-196`, `246-249`); tilt plumbed but dropped by `Element` (pos+pressure only, `lib.rs:51-53`) | Tilt fields preserved at boundary for future use; pressure clamped 0..1 | HUI owns arbitration per PROVENANCE (no HUI input routing exists yet for Canvas) | No stylus/mouse/touch/pad shortcuts; tilt accepted-then-dropped | BLOCKED BY HUI | Bridge could forward modifiers later; arbitration is HUI's job by design |
| Keyboard accelerators (app-level) | `appwindow/actions.rs:1108-1149` | Ctrl+O/S/Shift+S/T/Z/Shift+Z/C/X/V/B/I/U/P/L, F11/F9/F10, Ctrl+1..6 pen styles, Ctrl+0/±/KP zoom, Ctrl+Shift+A/R pages, Ctrl+Shift+P snap, etc. | `setup_action_accels`, `win.*` actions | None | None | None | No Canvas keyboard map | BLOCKED BY HUI | Needs HUI key routing + bridge ops underneath |
| Document layouts (FixedSize/ContinuousVertical/SemiInfinite/Infinite) | Canvas menu + doc prefs UI; `document/layout.rs:19-28` (default Infinite) | Page model choice; fixed-width constraint (`is_fixed_width`) | `Layout`, `Document::config`, `pages_bounds_w_content` | None — engine default layout used blindly | `document_bounds()` exposes storage extent only | None | No layout choice or page UI | BLOCKED BY BRIDGE | Affects export pagination + viewport mapping |
| Background color | Doc prefs / canvas menu; `document/background.rs:337-349` (default WHITE) | Page color | `Background::color` | None (`render_frame` uses engine default) | None | None | No background control | BLOCKED BY BRIDGE | — |
| Pattern style / size / color + with_background/with_pattern toggles | Doc prefs + export dialogs; `background.rs:PatternStyle` (None/Lines/Grid/Dots/IsometricGrid/IsometricDots, default Dots), `PATTERN_SIZE_DEFAULT 32x32`, `PATTERN_COLOR_DEFAULT` | Grid/dots/iso overlays; per-export toggles; print optimization | `Background::gen_svg`, `DocExportPrefs`, `DocPagesExportPrefs`, `SelectionExportPrefs` | None — `render_frame` hardcodes default prefs path (`lib.rs:415-454` passes prefs `with_background/with_pattern/optimize_printing` from `DocExportPrefs::default()` into `gen_svg`; not caller-configurable) | None | None | SVG always default background+pattern; no toggles | BLOCKED BY BRIDGE | Second-biggest visual gap after tools |
| Page format / size / DPI; add/remove page | `actions.rs:76-78`; doc config UI; `document/format.rs`, `config.rs` | Fixed-size page geometry, page list ops | `Format::size/dpi`, page add/remove engine ops | None | None | None | No page model in CakeOS | BLOCKED BY BRIDGE | — |
| Resize-to-fit-content / return-to-origin-page / clear-doc | `actions.rs:80-94` (+accels Ctrl+L, Ctrl+Shift+A/R) | One-shot doc ops | Engine doc ops | None | None | None | Absent | MISSING | Straightforward future bridge ops |
| Zoom (range, in/out/reset/fit-width, drag-zoom, pinch) | `canvasmenu.rs` (zoom_in/out/reset/fit_width buttons, reset label shows %); `camera.rs:76-83` (MIN 0.2, MAX 6.0, DEFAULT 1.0); `tools/zoom.rs`; `touch`/pinch prefs | Zoom with clamp, % readout, fit-width, temporary drag zoom | `Camera::zoom_to` (clamps), `zoom_temporarily_to`, `zoom_w_timeout`, `set_scale_factor` | `cake_canvas_zoom_to` + `set_viewport_size` + `pan_by` + (`set_viewport_center`/`get_viewport` exist in `ffi.rs:343-382` but are NOT bound in `CanvasNativeBridge.cs`) | `zoom_to`/`pan_by`/`set_viewport_size` proven in boundary proof; clamp inherited from engine | No zoom UI in toolstrip | Zoom/pan engine-real via API; no zoom buttons, %, fit-width, pinch | PARTIAL | C# gap: bind the two existing FFI viewport fns; then HUI zoom controls |
| Pan / offset / viewport center+size / nudge | `canvaswrapper.rs`; `tools/offsetcamera.rs`; `camera.rs:101-123,154-180,257-280,323-368` (offset, size, viewport, center, nudge+directions, overshoot 96px) | 2D pan, viewport tracking, edge nudge, overshoot | `Camera` full API | `set_viewport_size`, `pan_by` bound; `set_viewport_center`/`get_viewport` in FFI but unbound in C# | `set_viewport_center`/`viewport()` exist in Rust (`lib.rs:373-388`) | No pan UI (PoC drives via API) | Pan/viewport engine-real; center get/set unreachable from C# | PARTIAL | Same fix as Zoom row |
| Pressure + tilt stylus input | `canvaswrapper.rs` pen events; `rnote-compose` `Element{pos, pressure}` | Pressure-sensitive width; tilt plumbed at CakeOS boundary | `PenEvent::{Down,Up}` + `Element::new(pos, pressure)` | Pressure forwarded + clamped; tilt accepted in ABI struct then dropped (`ffi.rs:48-64`, `lib.rs:51-53` documented) | `CanvasPointerSample` keeps tilt for future engine support | HUI owns devices (no Canvas input routing yet) | Pressure works (proof varies 0.25→0.85); tilt no-op by engine design | PASS | Honest lossy point, documented in `lib.rs:26-30` |
| Touch drawing / pinch-zoom block / pen sounds / left-right handed | `actions.rs:149-155` (righthanded, touch_drawing, focus_mode, pen_sounds, snap, pinch block); `appmenu.rs:86-98` handedness toggles; `audioplayer.rs` | Touch-vs-pen arbitration prefs, sound feedback, handedness layout | `EngineConfig`, `AppSettings`, audio player | None — deliberately HUI-side per PROVENANCE | None | None | No touch/pref UI | BLOCKED BY HUI | By design HUI must own this; engine stays headless |
| Snap positions / respect-borders / autosave / focus mode / overview | `actions.rs:155-167`; canvasmenu fixed-size box; overlays | Canvas aids + modes | Engine/widget flags, `Snap`, workspace settings | None | None | None | Absent | MISSING | Future HUI prefs + thin bridge where engine-backed |
| New document / new tab / tab move-left/right / tab close | `actions.rs:56,96,129-133` (+accels Ctrl+T/W) | Multi-doc tabs | `RnCanvas` per-tab engine + workspace file tracking | Each `HeadlessCanvasEngine` is one doc; no tab model | Atomic save helper exists (`lib.rs:467-493`) but no tab/session model | None | Single-doc session only | BLOCKED BY HUI | Tabs are HUI composition; bridge already multi-instance capable |
| Open `.rnote` | `dialogs/import.rs:19-57` (`filedialog_open_doc`, mime `application/rnote`) | File picker → new tab | `EngineSnapshot::load_from_rnote_bytes`, `engine.load_snapshot` | `cake_canvas_engine_from_rnote` + `FromRnote` bound and proven | `from_rnote`/`from_rnote_file` (`lib.rs:497-518`) | No file dialog | Open works at API level; no picker UI | PARTIAL | Needs HUI file broker; bytes path passes |
| Save / Save-As `.rnote` | `dialogs/export.rs:21-93` (`dialog_save_doc_as`); `actions.rs:98-100` (+accels Ctrl+S/Shift+S) | Save to output file or prompt; toast + progress feedback | `save_as_rnote_bytes` (`export.rs:320-340`) | `cake_canvas_save_rnote` + `SaveRnote` bound and proven; same-dir atomic replace in Rust (`lib.rs:467-493`) | `save_rnote`, `save_rnote_atomically` | No save UI | Save works at API level; no menu/shortcut/toast | PARTIAL | Same as Open |
| Print document | `actions.rs:104` (+accel Ctrl+P) | System print | GTK print op over rendered pages | None | None | None | Absent | MISSING | Needs OS print via HUI; no engine blocker |
| Import XOPP / PDF / SVG / PNG / JPEG / TXT (+PDF prefs +XOPP DPI +password dialog) | `dialogs/import.rs:59-704`; `engine/import.rs` | Import pickers; PDF page-range/width%/bitmap-vs-vector/scale/spacing/adjust-doc prefs; XOPP DPI pref; encrypted-PDF password loop | `load_in_pdf_bytes`, `load_in_xopp_bytes`, SVG/image/text loaders | None | None | None | No import at all | BLOCKED BY BRIDGE | Large gap; engine import API exists upstream of this bridge |
| Export document (SVG/PDF/XOPP + background/pattern/print-optimize/page-order + preview) | `dialogs/export.rs:95-409`; `engine/export.rs:15-105` (`DocExportFormat`, `DocExportPrefs`) | Export dialog with live `RnStrokeContentPreview`, format picker, file chooser | `export_doc`, `export_doc_as_{svg,pdf,xopp}_bytes`, `extract_document_content/pages_content` | SVG-bytes only via `render_frame`/`export_svg` (default prefs, whole-doc); no PDF/XOPP, no prefs, no chooser | `export_svg` + `render_frame` (`lib.rs:394-454`); bridge binds `render_frame` only | No export UI | Whole-doc SVG export passes; PDF/XOPP + prefs + preview missing | PARTIAL | PDF needs cairo surface path; XOPP needs xoppformat path — both engine-side already |
| Export pages (SVG/PNG/JPEG + scalefactor + JPEG quality + stem naming + dir chooser) | `dialogs/export.rs:411-824`; `engine/export.rs:107-204` (`DocPagesExportFormat`, `DocPagesExportPrefs`: scale 1.8, quality 85) | Per-page files with preview | `export_doc_pages`, `extract_pages_content(page_order)` | None | None | None | Absent | BLOCKED BY BRIDGE | — |
| Export selection (SVG/PNG/JPEG + margin + preview) | `dialogs/export.rs:826-1132`; `engine/export.rs:206-298` (default margin 12) | Selection-only files with preview | `export_selection`, `extract_selection_content` | None (depends on unreachable selection) | None | None | Absent | BLOCKED BY BRIDGE | Blocked behind selection actions too |
| Export engine state / engine config JSON (debug) | `dialogs/export.rs:1230+`; `actions.rs:59-62`; `engine/export.rs:399-404` (`export_state_as_json`) | Debug dumps | `export_state_as_json` | `debug_state_json` exists in Rust (`lib.rs:521-525`) but unbound in FFI/C# | Tests use it for assertions only | None | Test-only; no UI | MISSING | Keep test-only; not product parity |
| Undo / Redo + can_undo/can_redo | `penpicker.rs` undo/redo buttons; `actions.rs:64-66` (+accels Ctrl+Z/Shift+Z); engine history | Per-stroke history incl. eraser-restore | `engine.undo/redo/can_undo/can_redo` (+chrono/trash comps) | Fully bound (`ffi.rs:428-470`; `CanvasNativeBridge.cs:134-144,53-69`); mid-stroke undo rejected (`InvalidState`) | `undo`/`redo` with active-stroke guard (`lib.rs:295-319`) | Undo/Redo buttons + `SetHistory` | End-to-end pass incl. eraser-restore case | PASS | — |
| Clipboard paste (insert at position / context-menu paste) | `actions.rs:122-127` + `clipboard_paste(target_pos)` (`actions.rs:1153+`) | Paste images/selection content at cursor | Paste loaders (image/svg) | None | None | None | Absent | BLOCKED BY BRIDGE | Needs ABI + OS clipboard via HUI |
| Selection trash / duplicate / invert-color | `actions.rs:84-88`; selector overlay context menu (`contextmenu.rs`) | One-click selection ops | Selection + trash/store comps | None | None | None | Absent | BLOCKED BY BRIDGE | Same root cause as selection actions |
| Text Bold / Italic / Underline | `actions.rs:114-118` (+accels Ctrl+B/I/U) | Typewriter rich text | `TextStyle` | None (no typewriter) | None | None | Absent | BLOCKED BY BRIDGE | Falls with typewriter |
| Color picker | `colorpicker/` + `data/ui/colorpicker/*` | Stroke/fill/background color choice | `rnote-compose::Color` | None | None | Vendor color primitives exist but no Canvas color UI | No color UI | BLOCKED BY HUI | Bridge must also expose color setters first (so: bridge+HUI) — classified HUI because picker surface is the larger half |
| Stroke-width picker | `strokewidthpicker/` | Numeric width entry + slider | Width fields on Brush/Shaper/Eraser configs | None | None | `Slider` primitive exists, unwired | No width UI | BLOCKED BY HUI | Same dual dependency as color; surface is HUI |
| Shape / icon pickers (groupediconpicker, iconpicker) | `groupediconpicker/`, `iconpicker.rs` | Visual builder gallery | `ShapeBuilderType::to_icon_name/from_icon_name` | 4 builders, no icon mapping | None | No gallery | ShapeButton only; no gallery | BLOCKED BY HUI | Needs gallery + 9 more ABI builders |
| Unit entry (numeric fields) | `unitentry.rs` | Validated dimension entry | — (widget-level) | None | None | None | Absent | BLOCKED BY HUI | Pure HUI widget gap |
| Settings panel + pen-shortcut editor | `settingspanel/mod.rs`, `penshortcutmodels.rs`, `penshortcutrow.rs`; `actions.rs:40` open-settings | Edit prefs + shortcut map | `EngineConfig`, `PensConfig::register_shortcut/remove_shortcut`, `Shortcuts` | None | None | None | Absent | BLOCKED BY HUI | Needs HUI prefs surface + config ABI |
| Export live preview (`RnStrokeContentPreview`) + doc paintable | `strokecontentpreview.rs`, `strokecontentpaintable.rs` | WYSIWYG export preview tiles | `StrokeContent::gen_svg/gen_image`, thumbnail path (`export.rs:919-987`) | Whole-doc SVG bytes only; no thumbnail, no page strip, no margin/mode preview | None | No preview surface for Canvas | Preview renders via host SVG, not donor preview widget | PARTIAL | Host SVG compositing is the HUI-side substitute; preview prefs still missing |
| Overlays (toasts, progress pulse, view-in-file-manager) | `overlays.rs`; export/save flows | Async feedback for save/export/import | `tasks.rs`, oneshot channels | Synchronous blocking ABI (`block_on` in `ffi.rs`) — no progress channel | None | `Notifications` platform contract exists, unwired to Canvas | `CanvasManagedBoundaryProof` writes console lines, no toasts | BLOCKED BY HUI | Async progress needs bridge redesign + HUI notifications |
| Left/right sidebar visibility model | `sidebar.rs`; `mainheader.rs:93-99` reveal toggles | Show/hide tool panels | Widget flags | None | None | None | Absent | BLOCKED BY HUI | — |
| Zoom-percentage readout on canvas menu button | `canvasmenu.rs:98-108` (`refresh_zoom_reset_label`: `"{:.0}%"`) | Live % label | `Camera::zoom` | `viewport().zoom` in Rust, `get_viewport` in FFI, unbound in C# | None | No label | No readout | BLOCKED BY BRIDGE | One-line C# binding away from data; label itself is HUI |
| About / Donate / Keyboard-shortcuts dialogs | `actions.rs:42-46`; app menu resources | Static dialogs | App metadata | None | None | None | Absent | MISSING | Not drawing parity; lowest priority |
| Visual-debug + developer mode | `actions.rs:149-167`; `engine/visual_debug.rs`; `widgetflags.rs` | Debug overlays, devel menu (F-panel) | `VisualDebug`, task handles | None (test JSON is not visual debug) | None | None | Absent | MISSING | Keep out of product scope; test JSON covers assertions |
| Whole-document SVG render frame (host-composited) | Donor renders via `StrokeContent::gen_svg` + cairo (`rendering.rs`, `strokecontent.rs`); preview via paintable | Donor paints pages/tiles interactively | `extract_document_content().gen_svg(...)` + `wrap_svg_root`/`add_xml_header` | `cake_canvas_render_frame` bound; format=SVG(1) + space=Document(1) validated in C# (`CanvasNativeBridge.cs:146-177`) | `render_frame` carries doc-space bounds + normalized viewBox (`lib.rs:406-454`) | Host composites SVG bytes (PoC + HUI preview surface) | Proven incl. save/reload bounds-equality check | PASS | Deliberate whole-doc first slice; tiles/dirty-regions deferred per `lib.rs:141-144` |
| Native `.rnote` round-trip + same-dir atomic replace | Donor save/load via `RnoteFile` + snapshot (`export.rs:319-340`, `snapshot.rs`) | Lossless persist incl. all pens/config | `save_as_rnote_bytes`, `EngineSnapshot::load_from_rnote_bytes`, `load_snapshot` | `save_rnote`/`engine_from_rnote` bound; atomic replace in Rust (`save_rnote_atomically`) | `from_rnote_file` + tmp-rename with cleanup | No file UI; session owns bytes | `CanvasManagedBoundaryProof` round-trips bytes + asserts bounds equality | PASS | File-chooser UI still missing (see Open/Save rows); bytes layer passes |

## Coverage counts (this matrix)

52 data rows, tallied row-by-row from the PARITY STATUS column above:

- PASS 6: Brush tool select · Eraser trash mode · Pressure input · Undo/Redo · Whole-doc SVG render · .rnote round-trip
- PARTIAL 10: Pen-picker strip (5/6) · Brush styles (2/3) · Shaper builders (4/13) · Selector gesture without style choice · Zoom API without UI · Pan/viewport API (2 FFI fns unbound in C#) · Open (bytes pass, no picker) · Save/Save-As (bytes pass, no UI) · Export-doc (SVG only) · Export preview substitute (host SVG, no prefs preview)
- BLOCKED BY BRIDGE 18: Brush width/color/fill/builder · Shaper smooth/rough/highlight/constraints · Typewriter · Eraser split+width · Selection actions · Selection clipboard · Tools tool · Layouts · Background color · Pattern prefs · Page format · Import · Export pages · Export selection · Clipboard paste · Selection trash/dup/invert · Text B/I/U · Zoom-% data (FFI exists, C# unbound)
- BLOCKED BY HUI 12: Window chrome · PenMode/shortcut arbitration · Keyboard map · New/tab model · Touch/prefs/focus · Color picker · Width picker · Shape gallery · Unit entry · Settings/shortcut editor · Overlays/toasts · Sidebar visibility
- MISSING 6: Resize-to-fit/return-origin/clear · Snap/borders/autosave/focus/overview prefs · Print · Engine-state JSON UI · About/donate/shortcuts dialogs · Visual-debug/devel

Checksum: 6 + 10 + 18 + 12 + 6 = 52. The per-row status column is authoritative.

## What "faithful" means for the next worker

1. Bind the two already-implemented FFI fns in C# (`cake_canvas_set_viewport_center`, `cake_canvas_get_viewport`) — cheapest delta in this matrix.
2. Extend the C ABI in `ffi.rs` order: selector style + selection actions + clipboard; shaper remaining 9 builders + smooth/rough/highlight/constraints; brush width/color/fill/builder + textured; eraser split + width; typewriter (pen + text actions); background/pattern/layout/format prefs; import (xopp/pdf/bitmap/text) + export (pdf/xopp/pages/bitmap) + print path.
3. Reconstruct in HUI (no GTK): header/title/unsaved, left workspace browser + tabs, right pens-stack pages, pen picker 6th+ tools buttons, canvas/app menus, export/import dialogs with live preview, color/width/shape galleries, settings + shortcut editor, overlays/toasts, keyboard map, touch arbitration, zoom controls + % readout.
4. Keep the backend wins: engine-real strokes, history, SVG, and atomic `.rnote` persistence are proven — do not re-implement or emulate them.
